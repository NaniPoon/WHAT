function calculateSupply (age,amount){
console.log(`You will need ${age*amount} to last you until the ripe old age of ${age}`);
}

calculateSupply(9000,200);
calculateSupply(100,310);
calculateSupply(40000,9300);